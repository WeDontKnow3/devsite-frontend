import React from 'react';

export default function MonoEmoji({ children, label = '', className = '', style = {} }) { return ( <span role="img" aria-label={label} className={mono-emoji ${className}} style={style} > {children} ); }
